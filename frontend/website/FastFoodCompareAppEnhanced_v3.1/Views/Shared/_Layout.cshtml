@using FastFoodCompareAppEnhanced_v3_1
@using FastFoodCompareAppEnhanced_v3_1.Models
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Fast Food Compare</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/css/site.css" rel="stylesheet" />
    <link href="~/css/auth.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">FastFood Compare</a>

            <div class="ms-auto d-flex align-items-center">
                <a href="@Url.Action("Index","Cart")" class="btn btn-outline-primary btn-sm me-3">🛒 Giỏ (<span id="cartCount">@{
                    var cart = Context.Session.GetObjectFromJson<List<CartItem>>("CART_ITEMS");
                    int count = cart == null ? 0 : cart.Sum(c=>c.Quantity);
                    @count
                }</span>)</a>

                @if (Context.Session.GetString("Username") != null)
                {
                    <div class="me-3 text-end">
                        <div class="small-muted">Xin chào</div>
                        <div><strong>@Context.Session.GetString("FullName")</strong></div>
                    </div>
                    <a href="@Url.Action("Logout","Account")" class="btn btn-outline-danger btn-sm">Đăng xuất</a>
                }
                else
                {
                    <a href="@Url.Action("Login","Account")" class="btn btn-outline-primary btn-sm me-2">Đăng nhập</a>
                    <a href="@Url.Action("Register","Account")" class="btn btn-gradient btn-sm">Đăng ký</a>
                }
            </div>
        </div>
    </nav>
    <div class="container">
        @RenderBody()
    </div>
    <footer class="text-center mt-5 mb-4 text-muted">© 2025 FastFood Compare</footer>
</body>
</html>
