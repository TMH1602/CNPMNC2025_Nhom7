@using FastFoodCompareAppEnhanced_v3_1
@using FastFoodCompareAppEnhanced_v3_1.Models
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Admin Portal</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="~/css/site.css" rel="stylesheet" />
    <link href="~/css/auth.css" rel="stylesheet" />

    <style>
        /* CSS CỦA BẠN - DÙNG CHO AVATAR */
        .user-avatar {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background-color: #ff6a00;
            color: #ffffff;
            font-weight: 600;
            font-size: 1rem;
        }
        
        /* CSS CHỈ DÀNH CHO ADMIN: Xóa background và tạo cấu trúc 2 cột */
        body {
            background-image: none !important; 
            background-color: #f8f9fa; /* Nền xám nhạt */
        }
        .navbar-admin {
            background-color: #495057 !important; /* Màu tối hơn cho Navbar Admin */
            color: white !important;
        }
        .sidebar {
            width: 250px;
            min-height: calc(100vh - 56px); /* Chiều cao trừ navbar */
            position: fixed;
            z-index: 1000;
            top: 56px; /* Bắt đầu sau navbar */
            left: 0;
            overflow-x: hidden;
            background-color: #343a40; /* Màu nền sidebar */
            padding-top: 20px;
        }
        .content-wrapper {
            margin-left: 250px; /* Nội dung chính cách sidebar */
            padding: 20px;
            width: calc(100% - 250px);
        }
        .nav-link.active-admin {
            background-color: #007bff !important; /* Màu xanh nổi bật */
            color: white !important;
        }
    </style>
</head>
<body>
    
    <nav class="navbar navbar-admin navbar-expand-lg shadow-sm fixed-top">
        <div class="container-fluid"> 
            <a class="navbar-brand text-white" href="/Admin/Products">
                <i class="bi bi-person-workspace me-2"></i> FastFood Admin Portal
            </a>
            
            <div class="ms-auto d-flex align-items-center">
                
                <a href="/Menu/Index" class="btn btn-sm btn-outline-warning me-3 rounded-pill">
                    <i class="bi bi-house-door"></i> Quay về Web
                </a>

                <div class="d-flex align-items-center" id="admin-logged-in-state">
                    
                    <span class="d-flex align-items-center me-3 text-white">
                        <span class="user-avatar me-2" id="user-avatar-initial"></span>
                        <strong id="user-display-name"></strong>
                    </span>
                    
                    <button class="btn btn-sm btn-danger rounded-pill" id="logout-button">
                        Đăng xuất
                    </button>
                </div>
                
            </div>
        </div>
    </nav>
    
    <div class="d-flex">
        
        <div class="sidebar d-flex flex-column text-white">
            <ul class="nav nav-pills flex-column mb-auto">
                <li class="nav-item">
                    <a href="/Admin/Products" class="nav-link text-white active-admin">
                        <i class="bi bi-basket-fill me-2"></i> Quản lý Sản phẩm
                    </a>
                </li>
                <li>
                    <a href="/Admin/Orders" class="nav-link text-white">
                        <i class="bi bi-receipt me-2"></i> Quản lý Đơn hàng
                    </a>
                </li>
                <li>
                    <a href="/Admin/Users" class="nav-link text-white">
                        <i class="bi bi-people-fill me-2"></i> Quản lý Người dùng
                    </a>
                </li>
            </ul>
        </div>

        <div class="content-wrapper">
            <main role="main">
                @RenderBody()
            </main>
        </div>
    </div>


    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // 🔥🔥 ĐÃ SỬA LỖI ROUTING: TRỎ THẲNG VỀ URL MVC GỐC
        const ADMIN_LOGIN_URL = '/Account/Login'; 

        // =================================================================
        // LOGIC KHỞI TẠO VÀ GẮN SỰ KIỆN (Tải trạng thái Admin)
        // =================================================================

        document.addEventListener("DOMContentLoaded", function () {
            
            const displayNameSpan = document.getElementById("user-display-name");
            const avatarSpan = document.getElementById("user-avatar-initial"); 
            const logoutButton = document.getElementById("logout-button");
            
            const username = localStorage.getItem("username"); 

            // --- Logic Navbar Admin ---
            if (username) {
                // Đã đăng nhập: Hiển thị tên và avatar
                if(displayNameSpan) displayNameSpan.textContent = username; 
                if(avatarSpan && username.length > 0) avatarSpan.textContent = username.charAt(0).toUpperCase();
            } else {
                // Chưa đăng nhập: Chuyển hướng về trang Đăng nhập
                window.location.href = ADMIN_LOGIN_URL;
                return;
            }

            // --- Xử lý nút Đăng xuất ---
            if(logoutButton) {
                logoutButton.addEventListener("click", function (e) {
                    e.preventDefault(); 
                    
                    // Xóa tất cả các khóa liên quan đến Auth
                    localStorage.removeItem("username");
                    localStorage.removeItem("jwtToken"); 
                    localStorage.removeItem("frontend_cart"); 
                    
                    // Chuyển hướng về trang đăng nhập (Không còn lỗi 404)
                    window.location.href = ADMIN_LOGIN_URL; 
                });
            }
        });
    </script>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>